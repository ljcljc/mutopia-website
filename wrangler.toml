# Cloudflare Pages Functions 配置
# 此文件用于配置 Cloudflare Pages Functions 的部署

name = "mutopia-pet"
compatibility_date = "2024-01-01"

# Pages 配置
pages_build_output_dir = "dist"

# 注意：
# - functions/ 目录必须在项目根目录
# - Cloudflare Pages 会自动识别 functions/ 目录
# - 如果使用 Git 集成，确保 functions/ 目录被提交到仓库
