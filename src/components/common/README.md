# Common Components

此文件夹用于存放通用的自定义组件，这些组件可以在项目的多个地方复用。

## 组件分类建议

- **UI 基础组件**: 自定义的按钮、输入框、卡片等基础 UI 组件
- **业务通用组件**: 跨业务模块使用的通用业务组件
- **工具组件**: 如加载状态、错误提示、空状态等工具类组件
- **布局组件**: 通用的布局容器组件

## 使用规范

1. 组件应该是可复用的，不包含特定业务逻辑
2. 组件应该具有良好的 TypeScript 类型定义
3. 组件应该包含必要的文档注释
4. 组件导出统一通过 `index.ts` 文件管理
